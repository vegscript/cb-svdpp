metadata:
  status: stage0_transfer
  owner: repo
  purpose: model_profile
  provenance:
    transferred_from: configs/models/tuned/ml1m_biased_mf_stage0_transfer.yaml
    transfer_stage: ml10m_stage0

model:
  name: biased_mf
  family: matrix_factorization
  scope: paper_inspired

training:
  latent_dim: 64
  epochs: 25
  learning_rate: 0.0075
  lambda_b: 0.03
  lambda_p: 0.03
  lambda_q: 0.03
  dtype: float32

notes:
  - transferred from the clean ml1m stage0 transfer profile
  - not tuned on ml10m
  - first ml10m feasibility readout only
  - not benchmark-final
