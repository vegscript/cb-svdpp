metadata:
  status: stage0_transfer
  owner: repo
  purpose: model_profile
  provenance:
    transferred_from: configs/models/tuned/ml100k_biased_mf_stage1.yaml
    transfer_stage: ml1m_stage0

model:
  name: biased_mf
  family: matrix_factorization
  scope: paper_inspired

training:
  latent_dim: 64
  epochs: 25
  learning_rate: 0.0075
  lambda_b: 0.03
  lambda_p: 0.03
  lambda_q: 0.03
  dtype: float32

notes:
  - transferred from the clean ml100k stage1 tuned profile
  - not tuned on ml1m
  - first scaling readout only
  - not benchmark-final
